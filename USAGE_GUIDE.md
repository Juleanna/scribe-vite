# 📝 Подробный гайд по работе с приложением

## 🚀 Быстрый старт
1) 🔌 Запустите приложение (через `npm run dev` или Docker — см. `DOCKER_GUIDE.md`), откройте указанный URL в браузере.  
2) 🌐 Выберите язык интерфейса (RU/EN) в шапке.  
3) 🏷️ Кликните по названию проекта в шапке, чтобы переименовать сценарий.  
4) 🧭 При необходимости включите интеграцию с браузерным захватом кликов (кнопка с индикатором + хоткей `Alt+C`).  

## 🎛️ Панель управления (Controls)
- ✨ Автоописание шагов: включите переключатель, чтобы приложение добавляло текст к скриншотам автоматически.  
  - 🧠 Локальное распознавание: используйте локальное (без сети), либо отключите, чтобы использовать браузерное облачное распознавание.  
- 🖍️ Стиль аннотаций: выберите `Стрелка`, `Рамка` или `Оба` — выделение клика на скриншоте.  
- 🖱️ Запись по клику (интеграция с расширением): при включённом режиме клик-захвата запись стартует/останавливается вместе с событиями от расширения.  
- 🎙️ Диктовка описаний: кнопка с микрофоном включает/выключает диктовку; горячая клавиша `Alt+R`. Текст диктовки добавляется к последнему или редактируемому шагу.  
- 📸 Кнопки действий:
  - «Скриншот» — сделать единичный снимок экрана.  
  - «Автозахват» — делать скриншоты автоматически (остановка кнопкой «Stop»).  
  - «Запись» — записать видео с экрана (остановка кнопкой «Stop»).  
  - «Загрузить» — импортировать готовые файлы (изображения или видео).  

## 🧩 Работа со списком шагов
- ➕ После каждого захвата появляется новый шаг: номер, заголовок, описание, вложение (картинка/видео).  
- ✏️ Редактирование: нажмите карандаш → правьте заголовок/описание → «Сохранить».  
- 🔄 Пересчёт описания: для изображений при включённом автоописании есть кнопка «Сгенерировать ещё раз» (с локальным или облачным движком).  
- ⬆️⬇️ Порядок: стрелки «вверх/вниз» меняют последовательность.  
- 🗑️ Удаление: корзина удаляет шаг.  
- 🎞️ Типы медиа: шаг может быть картинкой или видео; видео помечено бейджем «Видео».  

## 📤 Экспорт результатов
- 📄 HTML — кнопка «HTML» в шапке: скачивается статическая страница с шагами и медиа.  
- 🧾 Markdown — кнопка «MD»: текстовая версия с ссылками на медиа.  
- 🖨️ PDF — кнопка «PDF»: открывается окно печати/сохранения в PDF.  
Экспорт-кнопки появляются, когда есть хотя бы один шаг.  

## 💾 Хранение данных
- 🗂️ Шаги и настройки сохраняются в IndexedDB браузера: при обновлении страницы прогресс восстанавливается.  
- 🎥 Видео сохраняются как Blob в IndexedDB и при загрузке получают временные ссылки (revoked при выходе).  

## ⌨️ Горячие клавиши
- `Alt+C` — включить/выключить клик-захват (браузерное расширение, если подключено).  
- `Alt+R` — старт/стоп диктовки описаний.  

## 🧭 Типовой рабочий поток
1) Включите «Автоописание» и выберите стиль аннотаций.  
2) Сделайте серию «Скриншот» или запустите «Автозахват» для пошагового сценария.  
3) При необходимости запишите «Видео» или загрузите файлы через «Загрузить».  
4) Отредактируйте заголовки/описания, переставьте шаги.  
5) Пересгенерируйте описание сложных шагов (кнопка под изображением).  
6) Экспортируйте в HTML/MD/PDF и отправьте пользователям или команде.  

## 🔧 Советы и заметки
- 📌 Если 8080 занят (при запуске в Docker), пробросьте другой порт (`-p 3000:80`) и открывайте нужный.  
- 🛡️ Локальное распознавание отключает сетевые вызовы, но может быть менее точным; для лучшего качества выключите переключатель «Локальное».  
- 🔇 Диктовка работает, если браузер поддерживает Web Speech API; иначе кнопка не появится.  
- 🧹 Перед долгой сессией закройте лишние вкладки с приложением — так меньше шансов конфликтов при работе с IndexedDB.  

Готово! Следуя этому гайду, вы сможете быстро записывать сценарии, редактировать их и экспортировать в удобный формат. Если нужно дополнить инструкцию или адаптировать под конкретный процесс — сообщите. 
